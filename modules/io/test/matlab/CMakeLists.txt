SET(TOOLBOX_DIRECTORY "${EXECUTABLE_OUTPUT_PATH}/swig/matlab/openma")
SET(TESTS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}")
SET(MTD_DIRS ${TOOLBOX_DIRECTORY} ${TESTS_DIRECTORY} "${CMAKE_CURRENT_BINARY_DIR}")

CONFIGURE_FILE(OpenMATDDPathIn.m.in ${CMAKE_CURRENT_BINARY_DIR}/OpenMATDDPathIn.m)
CONFIGURE_FILE(OpenMATDDPathOut.m.in ${CMAKE_CURRENT_BINARY_DIR}/OpenMATDDPathOut.m)

ADD_MATLAB_TESTDRIVER(matlab_io IOTest.m INCLUDES ${MTD_DIRS})